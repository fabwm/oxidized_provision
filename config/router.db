#following the map and vars_map from config file, you have to follow this config format: IP:model:group:ssh_port, replace those params with the information from your devices. That can be also changed on the config file
IP:model:group:ssh_port
